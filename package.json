{
  "name": "boxed-fp-ts-compat",
  "version": "1.0.3",
  "license": "MIT",
  "main": "index.js",
  "source": "index.ts",
  "types": "index.d.ts",
  "scripts": {
    "build": "yarn clean && mkdir dist && cp package.json README.md ./dist/ && tsc",
    "clean": "rm -fr dist && rm tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@swan-io/boxed": "^3.2.0",
    "typescript": "5.6.3"
  },
  "peerDependencies": {
    "@swan-io/boxed": ">=3.2.0"
  }
}
